# 复合型机器人中枢监控平台 设计文档

> 版本：V1.0
> 日期：2026-07-15
> 适用对象：复合型机器人（双臂机械臂 + 移动底盘）在工业质检与装配工厂场景下的集中监控

---

## 1. 项目背景与目标

### 1.1 背景

在工业质检与装配工厂中，复合型机器人（Dual-Arm Manipulator + Mobile Chassis，简称"双臂移动机器人"）承担着**移动作业**与**精细操作**的双重任务：

- **移动底盘（AMR/AGV）**：负责跨工位移动、自主导航、避障与对接充电；
- **双臂机械臂**：负责工件抓取、装配拧紧、视觉质检等精细操作；
- **双臂协同**：左/右臂协同搬运、左右手配合装配、视觉-力觉融合控制。

由于机器人本体传感器众多（关节、视觉、力觉、激光雷达、IMU 等），且部署数量随产线扩张而增长，传统单机调试方式已无法满足**全厂级**的运行可视化、故障预警、产能分析与工艺优化需求。需建设一套**中枢监控平台**，实现"一屏观全厂、一网管全机"。

### 1.2 设计目标

| 目标维度 | 具体要求 |
|---------|---------|
| **可视性** | 实时呈现全厂机器人位置、姿态、任务状态、健康度 |
| **可预警** | 故障发生前 5~30 分钟通过趋势异常提前告警 |
| **可追溯** | 关键工艺数据（拧紧扭矩、质检图像）保留 ≥ 1 年，支持回放 |
| **可分析** | 提供质检良率、装配节拍、OEE 等多维分析报表 |
| **可扩展** | 支持 200+ 台机器人接入，新增机型 ≤ 1 周完成适配 |
| **高可用** | 平台可用性 ≥ 99.9%，数据采集延迟 ≤ 500ms |

---

## 2. 系统概述

### 2.1 监控对象

```
┌─────────────────────────────────────────────────────────┐
│                  复合型机器人本体                         │
│  ┌───────────────┐    ┌─────────────────────────────┐  │
│  │   移动底盘     │    │        双臂机械臂            │  │
│  │  (AMR/AGV)    │    │   Left Arm  +  Right Arm    │  │
│  │ - 电池/电机   │    │ - 各 7 轴关节               │  │
│  │ - 激光雷达    │    │ - 末端夹爪 / 螺丝刀 / 视觉  │  │
│  │ - 导航/定位   │    │ - 六维力觉传感器            │  │
│  │ - 避障/急停   │    │ - 手眼相机                  │  │
│  └───────────────┘    └─────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 监控场景

| 场景 | 典型动作 | 关键监控指标 |
|------|---------|------------|
| **质检场景** | 移动至工位 → 双臂举起工件 → 视觉相机扫描 → AI 推理判废 | 检测节拍、缺陷率、模型置信度、图像清晰度 |
| **装配场景** | 移动至产线 → 一臂扶件、一臂拧紧 → 力矩闭环 → 序列校验 | 拧紧扭矩曲线、角度、漏拧/错拧、节拍 |
| **搬运场景** | 双臂协同抓取 → 底盘导航至目标工位 → 精准对接放料 | 抓取成功率、对接误差、导航耗时、电量消耗 |
| **充电场景** | 低电量自主返航 → 对接充电桩 → 充电完成复位 | 电量曲线、充电时长、对接成功率 |

---

## 3. 系统架构设计

### 3.1 总体架构（四层模型）

```
┌──────────────────────────────────────────────────────────────┐
│  应用层 (Application Layer)                                   │
│  大屏驾驶舱 / Web 控制台 / 移动端 App / 告警中心 / 报表中心   │
└──────────────────────────────────────────────────────────────┘
                              ▲
┌──────────────────────────────────────────────────────────────┐
│  平台层 (Platform Layer)                                      │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐  │
│  │ 数据接入 │ 流式计算 │ 数据存储 │ AI 分析  │ 业务服务 │  │
│  │ Gateway  │ Flink    │ TSDB+DB  │ 推理引擎 │ 微服务   │  │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘  │
└──────────────────────────────────────────────────────────────┘
                              ▲
┌──────────────────────────────────────────────────────────────┐
│  网络层 (Network Layer)                                       │
│  5G 专网 / 工业 Wi-Fi 6 / 千兆以太网  +  MQTT / OPC UA        │
└──────────────────────────────────────────────────────────────┘
                              ▲
┌──────────────────────────────────────────────────────────────┐
│  边缘层 (Edge Layer) —— 部署于机器人本体或边缘网关            │
│  传感器采集 → 边缘预处理 → 协议封装 → 上报                   │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 层级 | 组件 | 选型 | 说明 |
|------|------|------|------|
| 边缘层 | 通信协议 | MQTT 5.0 / OPC UA | 高频时序走 MQTT，工艺数据走 OPC UA |
| 边缘层 | 边缘运行时 | ROS 2 + Edge Agent | 复用机器人既有 ROS 2 生态 |
| 网络层 | 工业网络 | 5G 专网 + Wi-Fi 6 | 移动机器人走 5G，固定设备走 Wi-Fi/有线 |
| 平台层 | 消息中间件 | EMQX (MQTT Broker) | 支持百万级连接与规则引擎 |
| 平台层 | 流计算 | Apache Flink | 实时告警、窗口聚合 |
| 平台层 | 时序库 | TDengine / InfluxDB | 高频关节、电量数据写入 |
| 平台层 | 关系库 | PostgreSQL | 机器人元数据、任务记录 |
| 平台层 | 对象存储 | MinIO | 质检图像、拧紧曲线文件 |
| 平台层 | AI 推理 | Triton + TensorRT | 缺陷检测模型服务化 |
| 平台层 | 业务服务 | Spring Cloud / Go-Micro | 微服务架构 |
| 应用层 | 前端 | Vue3 + ECharts + Three.js | 3D 数字孪生用 Three.js |
| 应用层 | 大屏 | DataV / Grafana | 驾驶舱与运维看板 |

---

## 4. 功能模块设计

### 4.1 模块总览

```
中枢监控平台
├── 1. 设备管理       (机器人台账、机型适配、上下线)
├── 2. 实时监控       (位置地图、姿态 3D、状态面板)
├── 3. 任务监控       (任务队列、执行轨迹、节拍分析)
├── 4. 质检监控       (检测图像、缺陷统计、模型监控)
├── 5. 装配监控       (拧紧曲线、力矩校验、序列防错)
├── 6. 健康管理       (健康度评分、寿命预测、保养提醒)
├── 7. 告警中心       (实时告警、规则引擎、工单流转)
├── 8. 数据分析       (OEE、良率、节拍、TopN 报表)
├── 9. 数字孪生       (3D 实时映射、回放、仿真)
└── 10. 系统管理      (用户、权限、日志、配置)
```

### 4.2 设备管理模块

**职责**：维护全厂机器人台账与在线状态。

- **台账信息**：机器人编号、机型、双臂型号、底盘型号、出厂日期、归属产线、责任工程师；
- **机型适配**：通过"机型描述文件（JSON Schema）"声明各机型的数据点位，新增机型仅需上传描述文件；
- **上下线管理**：心跳超时（默认 30s）自动标记离线，重连后自动恢复；
- **分组管理**：按车间/产线/工位分组，支持多级树形结构。

### 4.3 实时监控模块

**职责**：一屏呈现全厂机器人运行态。

#### 4.3.1 厂区电子地图（2D/3D）

- 基于 SLAM 建图数据渲染厂区地图；
- 实时显示每台机器人位置（经纬度/栅格坐标）、朝向、移动速度；
- 颜色编码状态：绿色运行 / 黄色待机 / 红色故障 / 灰色离线 / 蓝色充电；
- 支持点击机器人查看详情卡片。

#### 4.3.2 单机状态面板

| 子系统 | 监控项 |
|--------|--------|
| **底盘** | 电量、电压、电流、电机温度、轮速、里程、当前位置、目标点、导航状态、避障事件、急停状态、对接状态 |
| **左臂** | 7 关节角度/速度/扭矩/温度、末端位姿、夹爪开合、负载、碰撞标志、伺服报警 |
| **右臂** | 同左臂（独立通道） |
| **双臂协同** | 协同模式（主从/对称/独立）、协同任务编号、坐标系标定状态 |
| **环境** | 手眼相机帧率、激光雷达点云帧率、网络 RTT、CPU/GPU 占用 |

#### 4.3.3 3D 数字孪生

- 基于 URDF 模型实时驱动 3D 模型，关节角延迟 ≤ 200ms；
- 支持任意视角观察双臂协同动作；
- 历史时刻回放（步进 100ms）。

### 4.4 任务监控模块

**职责**：监控机器人任务执行全流程。

- **任务队列**：展示待执行/执行中/已完成/失败任务列表；
- **执行轨迹**：在地图上叠加任务路径，标注起点、途经点、终点；
- **节拍分析**：单任务实际耗时 vs 标准节拍，超时高亮；
- **失败分析**：失败原因分类（导航失败 / 抓取失败 / 拧紧失败 / 视觉失败 / 通信中断）。

### 4.5 质检监控模块

**职责**：监控工业质检过程与结果。

- **实时图像**：展示当前质检工件图像、AI 框选缺陷位置、置信度；
- **缺陷统计**：按缺陷类型（划痕/脏污/变形/缺件/错件）分类统计，支持时间/产线/机型维度；
- **良率看板**：实时良率、累计良率、良率趋势曲线；
- **模型监控**：推理延迟、置信度分布、人工复判结果回流，用于模型迭代；
- **可疑样本库**：低置信度样本自动入库，供人工复核与标注。

### 4.6 装配监控模块

**职责**：监控装配工艺关键参数，确保装配质量。

- **拧紧曲线**：实时绘制扭矩-角度曲线，叠加目标窗口（目标扭矩 ± 公差）；
- **序列防错**：依据工艺 BOM 校验拧紧顺序、螺钉数量，漏拧/错拧即时报警；
- **力矩校验**：每颗螺钉的最终扭矩、角度、时间戳入库，支持单件追溯；
- **力觉监控**：六维力传感器曲线，异常接触力预警（防止压坏工件/夹具）；
- **装配节拍**：单工位装配耗时分解（移动/定位/抓取/对位/拧紧/卸料）。

### 4.7 健康管理模块

**职责**：从"事后维修"转向"预测性维护"。

- **健康度评分**：综合电量、温度、振动、报警次数等加权计算 0~100 分；
- **寿命预测**：基于历史数据训练关节减速箱、电池、夹爪寿命预测模型；
- **保养提醒**：依据运行时长/里程触发保养工单（如减速箱润滑、电池均衡）；
- **劣化趋势**：关键参数趋势图（如关节扭矩漂移、电池容量衰减）。

### 4.8 告警中心模块

**职责**：统一告警产生、分级、流转、闭环。

- **告警分级**：
  - **紧急**：急停按下、碰撞发生、伺服严重报警 → 电话/短信 + 大屏弹窗；
  - **重要**：电量 < 20%、关节温度 > 75℃、扭矩超限 → 短信 + 工单；
  - **一般**：节拍超时、对接失败、模型置信度低 → 工单 + 邮件；
  - **提示**：保养到期、版本可升级 → 邮件。
- **规则引擎**：基于 Flink CEP 实现复合规则（如"连续 3 次对接失败 且 电量 > 50%"）；
- **告警抑制**：同源告警 5 分钟内合并，避免风暴；
- **工单流转**：告警 → 派单 → 处理 → 验证 → 关闭，全程留痕。

### 4.9 数据分析模块

**职责**：提供多维报表支持运营决策。

| 报表 | 维度 | 用途 |
|------|------|------|
| OEE 报表 | 机台/产线/班次 | 评估综合设备效率 |
| 良率报表 | 缺陷类型/工位/时段 | 定位质量瓶颈 |
| 节拍报表 | 任务类型/工位 | 优化产线平衡 |
| 故障 TopN | 故障类型/机台 | 指导改进方向 |
| 电量报表 | 机台/时段 | 优化充电调度 |
| 移动效率 | 路径/拥堵点 | 优化厂区动线 |

### 4.10 数字孪生模块

- 厂区级 3D 场景：建筑、产线、机器人、AGV 路径实时渲染；
- 物理同步：位置、关节角、状态 1:1 映射；
- 历史回放：选择时间区间回放任意机台动作；
- 仿真预演（预留）：导入任务计划，预演路径冲突与节拍。

---

## 5. 数据流设计

### 5.1 数据采集层级

```
机器人本体传感器
    │ (ROS 2 topic / Modbus)
    ▼
边缘 Edge Agent (本机或网关)
    │ 采集 → 预处理(降采样/去噪/打包) → 协议封装
    ▼
MQTT/OPC UA 上报 (TLS 加密)
    │
    ├──► 高频时序数据 (关节/电量/力矩, 10~100Hz) ──► TDengine
    ├──► 工艺事件数据 (拧紧结果/质检结果, 事件触发) ──► PostgreSQL
    ├──► 媒体数据 (质检图像/曲线文件, 按需)        ──► MinIO
    └──► 告警/日志 (实时)                          ──► Flink → 告警中心
```

### 5.2 数据频率与保留策略

| 数据类型 | 采集频率 | 热数据保留 | 冷数据归档 |
|---------|---------|-----------|-----------|
| 关节角度/扭矩 | 50 Hz | 7 天 (原始) / 90 天 (1Hz 降采样) | 1 年 (1Hz) |
| 电池/电机温度 | 1 Hz | 90 天 | 3 年 |
| 拧紧曲线 | 100 Hz（事件期） | 1 年 | 5 年 |
| 质检图像 | 事件触发 | 90 天 | 1 年（带缺陷的永久） |
| 任务记录 | 事件触发 | 1 年 | 5 年 |
| 告警记录 | 事件触发 | 1 年 | 5 年 |
| 位置轨迹 | 5 Hz | 30 天 | 1 年 (10s 抽样) |

### 5.3 数据上报协议示例（MQTT Topic 设计）

```
factory/{factoryId}/robot/{robotId}/telemetry/chassis      # 底盘遥测
factory/{factoryId}/robot/{robotId}/telemetry/arm/left     # 左臂遥测
factory/{factoryId}/robot/{robotId}/telemetry/arm/right    # 右臂遥测
factory/{factoryId}/robot/{robotId}/event/task             # 任务事件
factory/{factoryId}/robot/{robotId}/event/inspection       # 质检事件
factory/{factoryId}/robot/{robotId}/event/assembly         # 装配事件
factory/{factoryId}/robot/{robotId}/event/alarm            # 告警事件
factory/{factoryId}/robot/{robotId}/heartbeat              # 心跳
```

Payload 统一采用 JSON，含 `timestamp`、`robotId`、`type`、`payload` 字段。

---

## 6. 接口设计

### 6.1 北向接口（供上层应用调用）

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 机器人列表 | GET | `/api/v1/robots` | 分页查询，支持过滤 |
| 机器人详情 | GET | `/api/v1/robots/{id}` | 含实时状态 |
| 实时状态推送 | WS | `/ws/v1/robots/{id}/state` | WebSocket 推送 |
| 历史轨迹 | GET | `/api/v1/robots/{id}/trajectory` | 指定时间区间 |
| 拧紧记录查询 | GET | `/api/v1/assembly/tightening` | 支持按工件 SN 追溯 |
| 质检记录查询 | GET | `/api/v1/inspection/records` | 含图像 URL |
| 告警查询 | GET | `/api/v1/alarms` | 支持状态/级别过滤 |
| 告警确认 | POST | `/api/v1/alarms/{id}/acknowledge` | 工单流转 |
| 报表导出 | GET | `/api/v1/reports/{type}` | Excel/PDF |

### 6.2 南向接口（接入机器人/设备）

- **MQTT 接入**：Edge Agent 通过 TLS 双向认证接入 Broker；
- **OPC UA 接入**：支持既有 PLC/控制器数据点位映射；
- **机型描述文件**：YAML 格式声明点位 → 主题映射，平台自动解析入库。

---

## 7. 部署架构

### 7.1 部署拓扑

```
                  ┌─────────────────────┐
                  │   工厂数据中心机房   │
                  │  (K8s 集群 + 存储)   │
                  │  - 平台全部微服务    │
                  │  - TDengine 集群     │
                  │  - PostgreSQL 主从   │
                  │  - MinIO 分布式      │
                  │  - Flink 集群        │
                  └─────────────────────┘
                          ▲
                          │ 5G 专网 / 工业以太网
            ┌─────────────┼─────────────┐
            ▼             ▼             ▼
      ┌──────────┐  ┌──────────┐  ┌──────────┐
      │ 边缘网关  │  │ 边缘网关  │  │ 边缘网关  │
      │ (车间 A) │  │ (车间 B) │  │ (车间 C) │
      └────┬─────┘  └────┬─────┘  └────┬─────┘
           │             │             │
        多台机器人     多台机器人     多台机器人
      (本机 Edge Agent)
```

### 7.2 高可用与扩展

- **K8s 部署**：微服务多副本，HPA 自动扩缩容；
- **数据库**：TDengine 3 节点集群、PostgreSQL 主从 + PgBouncer；
- **消息中间件**：EMQX 集群，支持 Broker 故障切换；
- **存储**：MinIO 纠删码，容量按年增长 30% 预留；
- **灾备**：核心数据每日全量备份 + 增量备份至异地。

---

## 8. 安全设计

| 层面 | 措施 |
|------|------|
| **网络** | 5G 专网切片隔离、VLAN 划分、工业防火墙、单向网闸（必要时） |
| **接入** | MQTT TLS 双向认证、设备证书签发与轮换、OPC UA 安全策略 |
| **数据** | 传输加密 (TLS1.3)、敏感字段加密存储、图像水印防泄露 |
| **应用** | RBAC 权限模型、操作审计日志、SSO 单点登录、会话超时 |
| **运维** | 堡垒机接入、密钥管理 (Vault)、变更走 CI/CD 审批 |
| **应急** | 急停信号独立硬线通道，不依赖平台；平台故障不影响机器人本地运行 |

---

## 9. 非功能性需求

| 维度 | 指标 |
|------|------|
| 可用性 | ≥ 99.9%（年度停机 ≤ 8.76h） |
| 采集延迟 | 端到端 ≤ 500ms（95 分位） |
| 大屏刷新 | ≤ 1s |
| 告警延迟 | 事件发生 → 告警触达 ≤ 3s |
| 并发接入 | 支持 200 台机器人 × 50 点位/台 @ 10Hz |
| 数据查询 | 30 天内任意机台轨迹查询 ≤ 2s |
| 存储 | 单台机器人日均数据量 ≤ 2GB（含图像） |

---

## 10. 实施路线（建议分期）

| 阶段 | 范围 | 里程碑 |
|------|------|--------|
| **一期：可视** | 设备接入 + 实时监控 + 大屏 | 全厂机器人"看得见" |
| **二期：可控** | 任务监控 + 告警中心 + 工单 | 异常"管得住" |
| **三期：可析** | 质检/装配专项 + 报表中心 | 质量"算得清" |
| **四期：可智** | 健康预测 + 数字孪生 + AI 优化 | 运维"预得准" |

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 5G/Wi-Fi 信号盲区导致数据丢失 | 监控断续 | 边缘 Agent 本地缓存 + 断点续传 |
| 机器人厂商协议不统一 | 接入成本高 | 制定统一接入规范 + 机型描述文件机制 |
| 质检图像数据量大 | 存储膨胀 | 边缘预筛选 + 缺陷样本优先保留 + 分级存储 |
| 平台故障影响产线 | 停线风险 | 平台只读监控，控制权留在机器人本地；急停独立硬线 |
| 模型误判导致漏检 | 质量风险 | 低置信度强制人工复判 + 模型持续迭代 |

---

## 12. 附录

### 12.1 术语表

| 术语 | 含义 |
|------|------|
| AMR | Autonomous Mobile Robot，自主移动机器人 |
| AGV | Automated Guided Vehicle，自动导引车 |
| OEE | Overall Equipment Effectiveness，设备综合效率 |
| CEP | Complex Event Processing，复杂事件处理 |
| URDF | Unified Robot Description Format，机器人统一描述格式 |
| SLAM | Simultaneous Localization and Mapping，同步定位与建图 |
| RBAC | Role-Based Access Control，基于角色的访问控制 |

### 12.2 待确认事项

1. 全厂机器人具体品牌与型号清单（用于机型描述文件首批适配）；
2. 既有 MES/QMS 系统是否需要对接（影响数据回传接口设计）；
3. 质检图像是否需要长期留存以满足行业合规要求（影响存储容量规划）；
4. 是否需要支持远程接管机器人（影响安全与网络延迟设计）。

---

*文档结束*
